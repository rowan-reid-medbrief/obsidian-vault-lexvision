# Auto-generated by /promote-graph. Regenerable intermediates written by a /graph-to-notes
# run against a stored graph are ignored, so the committed store stays the curated set.
*/proposals.json
*/reconciliation.json
# R6/G verdict harvest (apply_insight --capture-verdicts): a 0600 calibration log holding
# verbatim note/meeting text, kept out of the committed tree (machine-local, private).
.harvest/
