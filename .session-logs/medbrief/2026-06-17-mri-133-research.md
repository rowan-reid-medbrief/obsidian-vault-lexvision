---
date: 2026-06-17
project: /Users/Rowan.Reid/claude_code/medbrief-onboarding
domain: medbrief
session: Independent research on MRI-133 (granular storing of pages) for Deon, with notes/14, a Lucid diagram and a branded docx
type: handoff
tags: [mri-133, medbrief, onboarding, harden-plan, lucid, medbrief-brand, handoff]
repos:
  - path: /Users/Rowan.Reid/claude_code/medbrief-onboarding
  - path: /Users/Rowan.Reid/.claude
  - path: /Users/Rowan.Reid/ObsidianVault
model_check: not-needed
---

## Session Summary

Independent, code-grounded research on Jira MRI-133 "granular storing of pages" for Deon (CTO) to compare against his own assessment. Key reframing: a stable page identity already exists (`Page.id`, which the sort already uses via `originalPageId`), so the ticket is a reference-rebinding + page-lineage problem, not identity-creation; the breaks are annotations binding to `Document.id` + XFDF position rather than `Page.id`, and split/clone minting new page rows with no lineage link. Produced `notes/14` (layered front/deep), a Lucid diagram, and a MedBrief-branded docx (with the diagram link embedded); drafted a covering Teams message to Dion. Plan hardened with a six-critic + pre-mortem fan-out before writing.

## Continuation Prompt

## Continuation: MRI-133 research: act on Dion's response

**Parent session:** 2026-06-17-mri-133-research.md

### Context
At the Kumulus Regroup (2026-06-12), Deon (CTO) set me an independent research task on Jira MRI-133 "granular storing of pages": research the system and ticket without his input, bring ideas, so he can compare against his own assessment. This session produced that research. I am sharing it interim with Dion (who owns the MRI-133 build) over Teams.

### What was done this session
- Wrote `notes/14-mri-133-research.md`: a layered, code-grounded research note (front compare-with-Deon layer + deeper "detail behind the lean" reference layer), in plain terms for a newcomer.
- Key finding: a stable page identity already EXISTS (`Page.id`; the sort references it via `originalPageId` in `SortingSession.storeData`). So MRI-133 is a reference-rebinding + page-lineage problem, not identity-creation. The two real gaps: (1) annotations bind to `Document.id` + position inside the XFDF blob, not `Page.id`; (2) `ClonePage`/`SplitPage` mint new `Page` rows with no lineage link to the source (`ReplacePage` mutates in place). No remap logic exists today; no `Redaction` entity (redaction persistence is greenfield).
- Hardened the plan first via a six-critic + pre-mortem + external-source + oracle fan-out (`/harden-plan`).
- Built a Lucid diagram (doc id `2ac46a14-3f47-4f76-9949-f87d9d49c149`) and a MedBrief-branded docx (`output/MRI-133-research.docx`) with the diagram link embedded.
- Drafted a covering Teams message to Dion.

### Current state
- Committed: project `5fda361` (notes/14, SOURCES, output/ on main); `~/.claude` `939dfc8`; vault `9786560`.
- The Teams message draft was NOT sent by Claude; Rowan sends it.

### Where we left off
Waiting on the Rowan -> Dion -> Rowan exchange. Next session, Rowan will paste (a) the message he sent Dion and (b) Dion's response.

### Next steps
1. WAIT for Rowan to paste his message to Dion + Dion's reply. Do not start research until that exchange is in hand.
2. Read Dion's response: it may confirm, correct, or redirect the findings, especially the "promote `storeData` blob to relational" hypothesis and the lineage gap (both flagged as needing his knowledge of the write path).
3. Deepen or refine `notes/14` from the exchange: e.g. resolve the open thread (how the Apryse/PDFTron viewer maps a sorted position to the `documentId` it saves against, which lives in the JS viewer layer, not in the local clone), trace the `storeData` write path if Dion confirms the relational direction, or pivot if he redirects.
4. Regenerate the branded docx from the updated note (`output/MRI-133-research.spec.json` is the saved spec); the build needed a wrap-aware adapter (the deferred IDEAS item).

Deferred this session to IDEAS: medbrief-brand `md_to_spec` wrap-awareness fix (under `## 2026-06-17` in `~/.claude/docs/IDEAS.md`); run `/whats-next --central` to rank.

### Key references
- `notes/14-mri-133-research.md`; `output/MRI-133-research.docx` (+ `.spec.json`)
- `notes/04-medbrief-datamodel.md` section 4, `onboarding-pack-v1.md` section 6, `notes/05`, `notes/09`
- MSR clone: `repos/secure-review/medbrief/src/` -> `Entity/{Page,Annotation,SortingSession}.php`, `Transformers/SortingSessionTransformer.php` (`originalPageId` = `Page.id` at line 104), `Service/SortingSession/{ClonePage,SplitPage,ReplacePage}`, `Controller/AnnotationController.php`
- Lucid: https://lucid.app/lucidchart/2ac46a14-3f47-4f76-9949-f87d9d49c149/edit
- People: Deon Kuhn (CTO, set the task), Dion (owns the build), Chantel van Wyk (walkthroughs). Jira MRI-133 (parent MRI-89), now Blocked / On Hold.

### Notes for next session
- Independence guardrail still applies: keep Rowan's independent line visible even if Dion shares an existing MedBrief design (fold it in as comparison).
- Plain-terms register, Rowan's voice, written for a newcomer; run the voice-wire pass on any additions.
