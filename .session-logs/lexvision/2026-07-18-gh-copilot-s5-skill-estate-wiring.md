---
tags: [gh-copilot, programme, skill, estate-wiring, merge-gate, allowed-tools, taxonomy, registry]
repos:
  - ~/.claude @ main (idea log #wjy2mt); branch gh-copilot @ 7be7889 (s5 skill + estate wiring)
  - ObsidianVault (session log + memory mirror)
parent: 2026-07-18-gh-copilot-s4-implement-mode.md
model_check: not-needed
---

# gh-copilot s5: skill, references, estate wiring (M2 complete on the branch)

Shipped s5, closing M2 on the mock: SKILL.md (224 lines, `disable-model-invocation: true`, `allowed-tools: Read, Write, Bash` per Rowan's Option B with the wrapper as the sole boundary), references/modes.md carrying the envelope-seam reconciliation (the actual 20-field dispatch and 23-key preflight envelopes; `credits_reported` + `task_id` named as the plan delta), shakedown.md (STATUS: PENDING), the config example, gitignore line, EVAL-TAXONOMY record, MAINTENANCE section, an estate-net runner whose oracle executes (doctors a profiles copy, proves the banned-flag guard bites), and the regenerated registry.json. The independent merge-gate verified every doc claim against code; its one must-fix landed (modes.md states the `.github/agents` preflight warn is NOT built, deferred to s6). Estate gate PASS, zero new KNOWN-RED; hashes 8d6a429 + 7be7889. Finding 49 resolved as a documented s3 deferral, retired against the estate's bare-list allowed-tools rule; Rowan then surfaced that the agentskills spec standardises scoped allowed-tools and Claude Code documents it natively, so the SKILLS-SPEC revisit trigger effectively fired: logged #wjy2mt and added the zero-credit scoped-tools pilot as s6 shakedown probe 11. Next: s6 live adversarial smokes (fresh session, 300-credit cap), then s7 merge.
