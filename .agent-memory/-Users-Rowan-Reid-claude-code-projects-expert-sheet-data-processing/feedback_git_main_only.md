---
name: feedback-git-main-only-expert-sheet
description: "For this project, work only on main — commit directly to main, no feature branches"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 779e42b3-8a57-48d2-a3ba-f26709960e81
---

For the expert-sheet-data-processing project, Rowan wants to work **only on `main`**. Commit
directly to `main`; do not create feature/working branches for changes here.

**Why:** This is a small, single-owner data-processing project. Rowan finds branch-and-merge
overhead unnecessary and confusing for it, and explicitly asked to "only be working in main on
this project" (2026-06-04, after a change was committed on a branch and then merged back).

**How to apply:**
- When committing in this project, commit straight to `main` — skip the "branch first if on the
  default branch" step from the global CLAUDE.md. That global rule is overridden *here only*.
- Still only commit/push when Rowan asks; this preference is about branch strategy, not about
  committing unprompted.
- Other projects are unaffected — the global branch-first default still applies elsewhere. See
  [[project-expert-sheet-data-processing]].
