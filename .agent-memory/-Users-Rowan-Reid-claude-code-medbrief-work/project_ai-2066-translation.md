---
name: ai-2066-translation
description: "AI-2066 Compre translation spike; Azure sync refuses PDF (batch-only), processes in FR/NL not UK, silently half-translates mixed PDFs; non-PHI demo found digital vs scanned inputs fail differently; 2026-07-17: real-PHI staging sign-off obtained from Deon/Laura, real sample is a built-in bilingual benchmark; external deep-research pass reconciled into all four core docs, PLAN.md restructured into a sequenced Phase 3-6 roadmap; MedBrief's vendor-account governance process found (Chris Fuller, PID+Information Security Forum) for the AWS/Google/DeepL benchmark sign-ups"
metadata: 
  node_type: memory
  type: project
  originSessionId: ea60356e-b1be-47e5-8653-2b6c741085f0
---

Azure DevOps work item **2066** (MedBrief AI project), a Compre translation and pseudonymisation workflow spike assigned to Rowan. Full working detail in `docs/ai-2066/`; vault thread in `Next Up.md` under `## Now (Medbrief)`.

Five non-obvious findings, expensive to re-derive:

- **Residency: a uksouth Translator resource processes in France Central / West Europe, NOT the UK** (found 2026-07-16 in Microsoft's "Document translation data residency" table; `uksouth` falls under the "Europe (except Switzerland)" row). This **corrected the workstream's own earlier claim** that the translator was "UK-resident, which resolves the estate/residency concern" and that "region confirmed UK". The recorded bar was "UK **or** EU" so the spike is not blocked, but production is a UK-at-rest to EU-processing transfer needing DPA sign-off (for Deon, not engineering). Unverified lever: a gated-access Translator Docker container would process in-estate. **If any note or person says "region confirmed UK", it is wrong.**
- **Azure silently half-translates mixed digital/scanned PDFs.** The FAQ states it plainly: mixed documents get "only the digital portions translated", with no error raised. Medical records are exactly that shape (digital cover letter plus scanned notes), so the output is a plausible-looking artifact with untranslated content, which is worse than a crash for a medico-legal product. The FAQ's own mitigation is rasterising to fully-scanned before submission. Whether "mixed" means within-page or across-pages is unresolved and determines a pipeline branch; only the hands-on run settles it.
- **Azure Document Translation refuses PDF on the synchronous endpoint** (2026-07-10 hands-on test; `document:translate` returns `InvalidFormat/ContentType` on every api-version); it accepts text, Office and HTML only. PDF is **batch-only** (`document/batches`), staging source and target in **blob containers**. So the translation pass is inherently an async, storage-backed step (the `salli_func` Azure-Function precedent), not an inline call. **Evidence asymmetry worth knowing** (checked 2026-07-16): the blob requirement IS explicitly stated in Microsoft's docs (three places, verbatim), but the PDF refusal is **not stated anywhere**; the only documentary trace is PDF's absence from the Synchronous tab of the overview's format table. Rowan's own test is the stronger citation, so cite it rather than the vendor docs.
- **Azure access, the operative map** (full-estate check 2026-07-16; supersedes a mis-scoped 2026-07-15 finding that read as "Rowan cannot self-serve storage" full stop, when it only ever checked Azure DevTest). Rowan's four subscriptions: **Azure DevTest** (`65483d0a-…`, holds `clinicalrecord-translator`) gives him four resource-scoped roles and **no create rights**; **Foundation A.I.** and **MedBrief Analytics** give him **nothing**; **MedBrief Secure Review** (`7b66abb8-…`, production) gives him **Contributor at subscription root, unconditional**. So **he can create storage accounts and Cognitive Services resources in production himself** (`Microsoft.Storage` and `Microsoft.CognitiveServices` both Registered; no Deny policies, only Defender/ASC monitoring). He also holds **Owner** on `stgsalli3odev` (DevTest, Salli's account). Earlier notes also had the translator's subscription ID wrong, recording Secure Review's; corrected 2026-07-15.
- **The one ceiling everywhere: Rowan cannot create role assignments.** Contributor carries `notActions: Microsoft.Authorization/*/Write`. So the **SAS path is fully self-serve** (Contributor's `actions: *` includes `listKeys`, and the harness already uses SAS, so **the spike needs nobody's permission**), but the **managed-identity path is blocked** and needs Owner/User Access Administrator to grant the Translator's identity blob access. That is the only real permission ask in the whole design, and it is a production concern, not a spike one. Exception: on `stgsalli3odev` he has Owner and could wire MI there.
- **So "Rowan is blocked on storage" was always wrong**, and the staging decision paused since 2026-07-10 is **a governance question, not a permissions one**: should spike resources holding PHI be created in the production subscription? His to raise, not to be granted. The blocker was an artefact of assuming staging had to sit beside the DevTest translator; SAS URLs are subscription-agnostic, so Secure Review storage works with the DevTest translator today. Secure Review is also where the PHI already lives (`msrstandard`), where `comprepseudo` already sits, and where the design said production belongs anyway.

**The reframe worth remembering** (raised 2026-07-16, unanswered): the translated-PDF premise is what forces the whole batch → blob → staging → cross-border → mixed-document chain. MedBrief already OCRs every document, and the sync API takes text natively with none of that machinery. AC5's "existing English translated pages **alongside** the German" suggests the current human workflow produces parallel pages, not an in-place PDF swap, so the premise may be dissolvable. Cheap to ask Deon, expensive to discover late.

**State at 2026-07-16 session end.** No batch run executed, no PHI has left the machine. The spike is unblocked technically and waits only on a governance answer. A Teams message to Deon and Laura is **drafted and unsent** at `docs/ai-2066/DRAFT-teams-message.md`: it reports the batch-only finding, states Rowan will create staging in Secure Review himself, and asks the two things he cannot answer alone (is there an intended harness already? and does the FR/NL processing need a DPA check?). Unresolved in that draft: whether to split the residency question into its own message to Deon, and whether to trim for length.

**A methodological lesson worth carrying**: twice in this workstream a confident recorded claim turned out to be wrong on checking, and both were load-bearing ("region confirmed UK"; "Rowan cannot self-serve storage"). Both came from checking one thing and generalising. When a claim in these notes blocks or unblocks work, re-derive it rather than inheriting it.

**2026-07-17: a non-PHI demo run, done in parallel with the still-open governance question above.** Built a synthetic German test PDF (fictitious patient, neutral invented name after a Mustermann-style placeholder proved a false start — see below) and a dedicated demo storage account (`stgtranslationdemo`, `rg-translation-demo`, Secure Review) to prove the batch/blob mechanism and produce a shareable before/after for Laura and Deon, without needing the governance sign-off. Two findings, each reproduced twice: on **clean digital text**, a whole sentence silently vanished from the translation with no error — a broader risk than the mixed-document FAQ finding above, since this document wasn't mixed at all. On a **rasterised, image-only (scanned) version of the identical content**, that same sentence survived, but the OCR path introduced its own visible defects instead (line-fragmented capitalisation, lost bold/colour formatting, a degree-symbol misread, one OCR-garbled German phrase left untranslated). **Digital and scanned inputs fail in different ways, not just differently-severe ones** — worth remembering before assuming either input shape is the "safe" one, and before generalising from a demo run on one shape to a claim about the other. Full detail: `docs/ai-2066/PLAN.md` Phase 2b, `DESIGN-NOTES.md` §10, `SOURCES.md`.

**Naming lesson for any future MT test fixture:** a synthetic test document's placeholder name followed the Mustermann convention (Germany's "John Doe") at first, and Azure translated it literally ("Model Clinic", "John Doe") since "Muster" is also an ordinary German word — correct translation, but a confusing artefact in front of stakeholders. Regenerated with a neutral invented name before showing anyone. Worth defaulting to a neutral name from the start next time, for this or any adjacent pseudonymisation/translation demo work.

**2026-07-17 check-in with Deon and Laura (narrated, not recorded).** Two developments. First, the real-PHI staging governance question open since 2026-07-10 is resolved: Rowan demonstrated his Secure Review blob storage and asked directly whether uploading the real German record was okay; both Deon and Laura said yes. Second, new information from Deon and Laura: the real sample document is not purely German, it alternates a few pages of German with the English translation of those same pages, repeated through the document — a built-in benchmark. Agreed idea, not yet executed: extract the German pages, translate them, and compare against the document's own embedded English as ground truth. Rowan also showed the digital-path demo defects (untranslated banner, dropped Diagnosis sentence, lost signature line breaks); everyone found it worrying, and the agreed next step is to benchmark AWS/Google/DeepL on synthetic data before any real PHI goes through them. **Discrepancy flagged, not resolved:** Rowan recalled the untranslated-banner and lost-signature-newline problems as belonging to the digital-path test; the findings above have those on the scanned/OCR-path test instead, with the digital path recorded as preserving formatting apart from the dropped sentence. Worth checking which variant was actually on screen before treating this as a new digital-path finding. Full detail: `docs/ai-2066/meetings/2026-07-17-deon-laura-checkin.md` (a narrated recollection, not a transcript).

**2026-07-17: MedBrief's vendor-account governance, found via Confluence (relevant to Phase 3 item 4, the AWS/Google/DeepL trial sign-ups).** No documented self-service process exists. **Chris Fuller** (IT, Compliance, and Business Success) owns new SaaS accounts and sits on the quarterly **Information Security Forum** (CEO Adam Stead chairs; CTO Deon Kuhn, SecOps Zac Dreyer also attend) that reviews new tool acquisitions via a **PID** (Project Initiation Document). Deon's 10 July steer ("sign up as needed, tell Chris") is enough for synthetic-only testing; real PHI through a new vendor would likely need the heavier PID route, by precedent (MedBrief already ran a formal governance review for ChatGPT/M365 connections). MedBrief holds **no existing AWS or DeepL account**, and possibly no Google account either (unconfirmed, ask Laura). All three vendors require a payment card at sign-up even to stay on the free tier.

**2026-07-17: an external deep-research pass reshaped the workstream, and PLAN.md was restructured the same day.** A brief (`docs/ai-2066/RESEARCH-BRIEF.md`) handed to GitHub Copilot returned findings (`RESEARCH-FINDINGS.md`), reconciled into `OVERVIEW.md`, `SOURCES.md`, `BENCHMARK.md`, and `DESIGN-NOTES.md` (new §17-§18). Headlines, expensive to re-derive: it independently reaches and extends the "does it have to be a translated PDF" reframe (bullet above), recommending translation and pseudonymisation share one structured-document layer, built for pseudonymisation first (higher priority per Deon), translation consumes it once it exists; it flags that the **currently-published Azure API version (`2026-03-01`) may have changed the batch PDF backend** since this workstream's tests were pinned to `2024-05-01`, so the mixed-document/omission findings above **need re-confirming, not yet done**; and it confirms an LLM-based route does **not** de-risk the silent-omission finding (can be worse for it) — corrects an overclaim made and caught mid-session. **`PLAN.md`'s old Phase 3/4 are gone**, replaced by a sequenced Phase 3 (cheap de-risking checks, do these first) through Phase 6 (build, explicitly split between what AI-2066 owns and what it only feeds into pseudonymisation's shared layer), plus a parallel compliance track that surfaced a new requirement: a **DPIA is very likely mandatory** (large-scale special-category data processing is an automatic ICO trigger), not previously identified anywhere in this ticket. Read `PLAN.md` fresh rather than assuming the old phase numbers.

Related: [[project_medbrief-prod-file-access]] (`msrstandard` Azure Files is where the real PDFs live, and the batch API cannot read Azure Files, only blob), [[project_mri-134-branch]] (sibling workstream, same repo).
